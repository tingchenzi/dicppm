# dic-crm3ppm-ui 使用说明

## 🎉 项目已创建完成！

独立的 PPM 插件项目已在以下位置创建：
```
e:\工作2\dic-crm3ppm-ui
```

## 📂 项目结构

```
dic-crm3ppm-ui/
├── src/                    # 源代码（需要复制 PPM 模块）
├── examples/               # 示例项目
├── docs/                   # 完整文档
├── scripts/                # 辅助脚本
├── package.json            # 项目配置
├── vue.config.js           # 构建配置
└── QUICK_START.md          # 快速开始指南
```

## 🚀 快速开始（3 步完成）

### 第 1 步：复制源代码

**方式 1：使用脚本（推荐）**
```bash
cd e:\工作2\dic-crm3ppm-ui
node scripts/copy-source.js
```

**方式 2：手动复制**
```
从: e:\工作2\crm3.0-Enterprise\src\crm3.0part7-ppm
到: e:\工作2\dic-crm3ppm-ui\src\crm3.0part7-ppm
```

### 第 2 步：初始化项目

**Windows 用户（推荐）**
```bash
双击运行: scripts\init-project.bat
```

**或手动执行**
```bash
cd e:\工作2\dic-crm3ppm-ui
npm install
npm run build
```

### 第 3 步：测试插件

```bash
npm run dev
```
浏览器会自动打开 http://localhost:8090，查看示例效果。

## 📦 发布到 Git 和 npm

### 发布到 Git

```bash
cd e:\工作2\dic-crm3ppm-ui

# 初始化 Git
git init

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/yourcompany/dic-crm3ppm-ui.git

# 提交代码
git add .
git commit -m "feat: 初始化 PPM 插件项目"
git push -u origin main

# 创建版本标签
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin v1.0.0
```

### 发布到 npm

**Windows 用户（推荐）**
```bash
双击运行: scripts\publish.bat
```

**或手动执行**
```bash
# 登录 npm
npm login

# 发布
npm publish

# 或发布到私有 npm
npm publish --registry=http://your-npm-registry.com
```

## 🔌 在其他项目中使用

### 1. 安装插件

```bash
# 从 npm 安装
npm install dic-crm3ppm-ui

# 从 Git 安装
npm install git+https://github.com/yourcompany/dic-crm3ppm-ui.git

# 从本地安装（开发测试）
npm install file:../dic-crm3ppm-ui
```

### 2. 创建 API 适配器

在项目中创建 `src/api/ppmAdapter.js`：

```javascript
import OrderService from '@/api/order/OrderService'
import phoneOrderServe from '@/api/order/phoneOrderServe'

export default {
  async getOrderInfo(params) {
    return await OrderService.getOrderInfo(params)
  },
  async saveForm(data) {
    return await phoneOrderServe.saveForm(data)
  },
  async executeSql(params) {
    return await OrderService.executeSql(params)
  },
  // ... 其他方法
}
```

### 3. 在 main.js 中注册

```javascript
import Vue from 'vue'
import ElementUI from 'element-ui'
import 'element-ui/lib/theme-chalk/index.css'
import DicPPM from 'dic-crm3ppm-ui'
import 'dic-crm3ppm-ui/lib/dic-crm3ppm-ui.css'
import apiAdapter from '@/api/ppmAdapter'

Vue.use(ElementUI)
Vue.use(DicPPM, {
  apiAdapter: apiAdapter
})
```

### 4. 在组件中使用

```vue
<template>
  <div>
    <ppm-form :form-data="formData" />
    <el-button v-prevent-re-click="handleSubmit">提交</el-button>
  </div>
</template>

<script>
export default {
  data() {
    return {
      formData: {}
    }
  },
  methods: {
    handleSubmit() {
      const cloned = this.$ppmUtils.deepClone(this.formData)
      this.$ppmApi.saveForm(cloned)
    }
  }
}
</script>
```

## 📚 完整文档

| 文档 | 说明 |
|------|------|
| [README.md](./README.md) | 项目介绍和基础用法 |
| [QUICK_START.md](./QUICK_START.md) | 快速开始指南（详细步骤） |
| [docs/API.md](./docs/API.md) | 完整 API 文档 |
| [docs/MIGRATION.md](./docs/MIGRATION.md) | 从旧项目迁移指南 |
| [docs/DEPLOYMENT.md](./docs/DEPLOYMENT.md) | 详细部署和发布指南 |
| [PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md) | 项目结构说明 |
| [CHANGELOG.md](./CHANGELOG.md) | 版本更新日志 |

## 🛠️ 常用命令

```bash
# 开发
npm run dev              # 启动开发服务器（示例项目）
npm run build            # 构建生产版本
npm run build:watch      # 监听模式构建
npm run lint             # 代码检查

# Git
git add .                # 添加所有文件
git commit -m "message"  # 提交
git push                 # 推送
git tag v1.0.0           # 创建标签

# npm
npm version patch        # 更新补丁版本 (1.0.0 -> 1.0.1)
npm version minor        # 更新次版本 (1.0.0 -> 1.1.0)
npm version major        # 更新主版本 (1.0.0 -> 2.0.0)
npm publish              # 发布包
```

## ✅ 检查清单

发布前请确保：

- [ ] ✅ 源代码已从原项目复制到 `src/crm3.0part7-ppm`
- [ ] ✅ 依赖已安装 (`npm install`)
- [ ] ✅ 构建成功 (`npm run build`)
- [ ] ✅ 本地测试通过 (`npm run dev`)
- [ ] ✅ package.json 中的仓库地址已更新
- [ ] ✅ README.md 已根据实际情况修改
- [ ] ✅ Git 仓库已创建并推送
- [ ] ✅ npm 包已发布
- [ ] ✅ 在测试项目中验证可用

## 🎯 核心特性

- ✨ **插件化架构** - 通过 Vue.use() 一键安装
- 🔌 **API 适配器** - 解耦业务逻辑，支持不同后端
- 📦 **多种引入方式** - 完整引入、按需引入、CDN 引入
- 🎨 **自定义指令** - v-fold、v-prevent-re-click、v-debounce
- 🛠️ **工具函数** - deepClone、deepAssign 等
- 📚 **完整文档** - API 文档、迁移指南、示例代码
- 🚀 **开箱即用** - 自动注册组件，无需手动导入

## 🔄 版本管理

遵循语义化版本规范：

- **主版本号 (MAJOR)**: 不兼容的 API 修改
- **次版本号 (MINOR)**: 向下兼容的功能性新增
- **修订号 (PATCH)**: 向下兼容的问题修正

## 💡 最佳实践

1. **API 适配器**: 为每个项目创建独立的适配器文件
2. **版本锁定**: 在 package.json 中锁定插件版本
3. **按需引入**: 如果只用部分组件，使用按需引入减小体积
4. **样式隔离**: 使用命名空间避免样式冲突
5. **错误处理**: 在适配器中添加统一的错误处理

## 🆘 常见问题

### Q: 构建失败怎么办？
**A:** 
1. 确保已复制源代码到 `src/crm3.0part7-ppm`
2. 检查 Node.js 版本（建议 14+）
3. 删除 `node_modules` 重新安装

### Q: 如何更新插件版本？
**A:**
```bash
# 1. 修改代码
# 2. 更新版本号
npm version patch
# 3. 构建
npm run build
# 4. 发布
npm publish
```

### Q: 如何在多个项目间共享？
**A:** 
- 方式1: 发布到 npm（推荐）
- 方式2: 发布到 Git，通过 Git URL 安装
- 方式3: 搭建私有 npm 仓库（Verdaccio）

## 📞 获取帮助

- 📖 查看文档
- 🐛 提交 Issue
- 💬 联系维护团队
- 📧 邮箱: support@yourcompany.com

## 🎊 恭喜！

你已经成功创建了一个可复用的 PPM 插件包！

现在可以在多个项目中使用它，实现代码复用和统一维护。
